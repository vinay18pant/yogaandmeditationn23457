import 'package:flutter/material.dart';
import 'package:meditation_app/widgets/bottom_nav_bar.dart';
class yogasan extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
          home: Scaffold(
            appBar: AppBar(
               title: Center(child: Text('Yogasan')),
                     ),

          ),

    );
  }
}
